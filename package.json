{
  "name": "437",
  "version": "1.0.0",
  "description": "Lab1",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "preview": "vite preview",
    
    "build": "npm run build --workspace=packages/app && npm run build --workspace=packages/server",
    
    "start:app": "npm start --workspace=packages/server"
  },
  "repository": {
    "type": "git",
    "url": "437"
  },
  "author": "Luis M",
  "license": "ISC",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "patch-package": "^8.0.1"
  }
}